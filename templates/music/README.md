# Music piece template

Copy `music/_template/` to `music/piece_slug/`.

Include instrumentation and duration in `hai-res:subtitle`. Add recordings to `assets/` and embed with `<audio>` or external links.
